{
  "name": "darkcloud",
  "version": "1.0.0",
  "description": "A dark SoundCloud user style.",
  "main": "index.js",
  "scripts": {
    "watch": "sass --watch src:dist --style compressed",
    "compile": "sass src:dist --style compressed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "stashymane",
  "license": "Apache 2.0",
  "dependencies": {
    "sass": "^1.62.1"
  }
}
