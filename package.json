{
  "name": "darkcloud",
  "version": "0.1.0-BETA",
  "description": "A dark SoundCloud user style.",
  "main": "index.js",
  "scripts": {
    "dev": "start firefox %cd%\\dist\\style.user.css && npm run watch",
    "watch": "sass --watch src:dist",
    "build": "sass src:dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "stashymane",
  "license": "Apache 2.0",
  "dependencies": {
    "sass": "^1.62.1"
  }
}
